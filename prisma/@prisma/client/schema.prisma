datasource db {
  provider = "postgresql"
}

generator client {
  provider = "prisma-client-js"
  output   = "@prisma/client"
}

model Product {
  id          String @id @default(uuid())
  userId      String
  name        String
  cost        Float
  cycle       String
  nextRenewal String

  @@map("product")
}
