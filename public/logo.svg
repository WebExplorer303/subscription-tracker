<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100">
  <!-- Solid Background -->
  <rect width="100" height="100" rx="20" fill="#0f172a"/>
  
  <!-- Outer Ring (Progress) -->
  <circle cx="50" cy="50" r="30" stroke="#38bdf8" stroke-width="12" fill="none" stroke-dasharray="140 60" />
  
  <!-- Inner Dot -->
  <circle cx="50" cy="50" r="8" fill="#ffffff"/>
</svg>
